"use client";
import { useEffect,useState } from "react";import Link from "next/link";import {ArrowLeft,CreditCard,RefreshCw} from "lucide-react";import {supabase} from "@/lib/supabase/client";
type Tx={id:string;amount:number;currency:string;status:string;description:string|null;created_at:string};
export default function Transactions(){const [items,setItems]=useState<Tx[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");async function load(){setLoading(true);const {data:{user}}=await supabase.auth.getUser();if(!user){location.href="/auth";return}const {data,error}=await supabase.from("transactions").select("id,amount,currency,status,description,created_at").order("created_at",{ascending:false}).limit(100);if(error)setError(error.message);setItems((data as Tx[])||[]);setLoading(false)}useEffect(()=>{load()},[]);return <main className="min-h-screen"><div className="mx-auto max-w-4xl px-5 py-10"><Link href="/dashboard" className="flex items-center gap-2 text-sm text-green-400"><ArrowLeft size={15}/> Dashboard</Link><div className="mt-6 flex justify-between"><div><h1 className="text-3xl font-black">Transactions</h1><p className="mt-2 text-slate-400">Your payment and service activity.</p></div><button onClick={load} className="rounded-xl border border-slate-700 p-3"><RefreshCw size={18}/></button></div>{error&&<p className="mt-5 rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-200">{error}</p>}<div className="mt-7 space-y-3">{loading?<p className="text-slate-400">Loading...</p>:items.length===0?<div className="card p-10 text-center"><CreditCard className="mx-auto text-green-400"/><h2 className="mt-3 font-bold">No transactions yet</h2></div>:items.map(t=><div key={t.id} className="card flex items-center justify-between gap-4 p-5"><div><p className="font-semibold">{t.description||"Service transaction"}</p><p className="mt-1 text-xs text-slate-500">{new Date(t.created_at).toLocaleString()}</p></div><div className="text-right"><p className="font-bold">{t.currency} {Number(t.amount).toFixed(2)}</p><span className="text-xs text-slate-400">{t.status}</span></div></div>)}</div></div></main>}
